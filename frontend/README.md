# TeleRAG Frontend

基于 Next.js App Router 与 shadcn/ui 风格组件构建的正式用户端。

## 启动

```bash
npm install
npm run dev
```

默认请求 `http://127.0.0.1:8000` 的 FastAPI 后端。

## 环境变量

```bash
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
```
